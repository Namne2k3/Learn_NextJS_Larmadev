/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "images.pexels.com",
            "example.com",
            "th.bing.com"
        ]
    }
}

module.exports = nextConfig
